Backend API

- Start: `npm run dev` (usa `tsx`)
- Build: configure a task `build` se necessário

Prisma:
- Schema está em `prisma/schema.prisma`
- Gerado em `generated/` (não commitar o client gerado; rodar `prisma generate` onde necessário)
